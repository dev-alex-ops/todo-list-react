pipeline {
    agent any
    stages {
        stage ('Get code') {
            steps {
                git 'https://github.com/dev-alex-ops/todo-list-react'
            }
        }

        stage ('Build Docker image') {
            steps {
                sh 'docker build -t todolist-app:1 .'
            }
        }
    }
}